
import SwiftUI

struct AppointmentsFilterView: View {
    var body: some View {
        Button("Show appointments by practices") {
            // Action handler
        }
        .padding()
        .background(Color.blue.opacity(0.1))
        .cornerRadius(10)
    }
}
